package io.github.ayfri.kore.helpers.displays.maths.internal

/** Minimal port of `org.joml.Vector3f`, only the members used by the display transformation math. */
data class Vector3f(var x: Float = 0f, var y: Float = 0f, var z: Float = 0f)
